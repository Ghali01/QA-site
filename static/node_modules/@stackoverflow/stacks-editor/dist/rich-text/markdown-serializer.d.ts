import { MarkdownSerializer, MarkdownSerializerState } from "prosemirror-markdown";
import { Node as ProsemirrorNode } from "prosemirror-model";
import { ExternalEditorPlugin } from "../shared/external-editor-plugin";
export declare type MarkdownSerializerNodes = {
    [name: string]: (state: MarkdownSerializerState, node: ProsemirrorNode, parent: ProsemirrorNode, index: number) => void;
};
export declare const stackOverflowMarkdownSerializer: (externalPlugin: ExternalEditorPlugin) => MarkdownSerializer;
